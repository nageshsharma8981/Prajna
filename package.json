{
  "name": "prajna",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "cd web && npm install --include=dev --no-audit --no-fund && npx vite build",
    "start": "node server/server.js"
  }
}
