{
  "name": "prajna",
  "private": true,
  "version": "1.25.0",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "cd web && npm install --include=dev --no-audit --no-fund && npx vite build",
    "start": "node server/server.js",
    "check": "node scripts/postdeploy-check.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "bin": {
    "prajna": "cli/prajna.mjs"
  }
}
